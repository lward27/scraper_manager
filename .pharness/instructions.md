Python 3.11 scheduler and worker service for the Finance Product. Source lives under src/ and tests under tests/. Keep startup configuration validation pure, deterministic, and completed before scheduler or worker services start. Preserve stable field-specific errors. Update README.md or DEPLOYMENT.md when configuration behavior changes. Use only the declared acceptance commands. Dependencies are installed only during PHarness preparation from the immutable hashed requirements.lock; coding-stage network and package installation are denied.